<table class="xsmall">
{% for match in matches %}
<tr class="small">
	<td>
		<span class="bright">{{ match.team1.name }}</span>
		<span> vs </span>
		<span class="bright">{{ match.team2.name }}</span>
	</td>
    {% if match.live %}
		<td class="align-right bright">{{ "LIVE" | translate }}</td>
	{% else %}
		<td class="align-right">{{ match.starts }}</td>
	{% endif %}
</tr>
<tr>
	<td colspan="3" class="align-left">{{ match.tournament }}</td>
</tr>
{% endfor %}
</table>
